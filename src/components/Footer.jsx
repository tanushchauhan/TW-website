function Footer() {
  return (
    <div>
      {/* add a footer that should have 3 section system. First logo, then name + copyright. Then end it with social logos */}
    </div>
  );
}

export default Footer;
